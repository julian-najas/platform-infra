# environments/dev/secrets.example.yaml
# Template de secrets para dev. NO COMMITEAR secrets reales.
# Copiar a secrets.yaml y rellenar los valores.

hmac_secret: "REPLACE_ME_dev_hmac_secret"
database_password: "REPLACE_ME_dev_db_password"
redis_password: ""  # Sin password en dev local
github_token: "REPLACE_ME_github_pat"

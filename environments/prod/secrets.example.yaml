# environments/prod/secrets.example.yaml
# Template de secrets para producción. NUNCA commitear secrets reales.
# Se gestionan via Sealed Secrets o External Secrets Operator.

hmac_secret: "REPLACE_ME_prod_hmac_secret"
database_password: "REPLACE_ME_prod_db_password"
redis_password: "REPLACE_ME_prod_redis_password"
github_token: "REPLACE_ME_github_pat"
